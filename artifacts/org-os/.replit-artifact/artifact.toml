kind = "web"
previewPath = "/"
title = "Organization OS"
version = "1.0.0"
id = "artifacts/org-os"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/org-os run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/org-os", "run", "build" ]
publicDir = "artifacts/org-os/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
